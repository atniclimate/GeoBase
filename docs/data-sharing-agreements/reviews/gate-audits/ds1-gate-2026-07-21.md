# DS-1 full gate audit — 2026-07-21

Lane: Claude verification subagent (full DS-1 gate, all six lanes merged: baseline,
wa-coastal, wa-inland, or, id, institutional). No-network audit; nothing fetched.
Worker outputs treated as inputs, not truths. The baseline-slice audit
(`ds1-baseline-2026-07-21.md`, PASS) is incorporated by reference; its 1:1
inventory work was not redone.

- Seed: **20260721** (`random.Random(20260721)`; strata and candidate lists sorted
  before sampling)
- Population: `sources/register.jsonl` (107 rows = 53+12+7+8+7+20 slice rows),
  `provenance/access-log.jsonl` (231 events = 106+37+19+17+12+36+4 slice events),
  `sources/coverage-matrix.jsonl` (43 rows), `sources/NATIONS.md` (43 Nations).

## Verdict: **PASS (after remediation — see addendum)**

Initial audit verdict was FAIL on one blocking finding in check 5 (unresolved
cross-lane duplicate pair src-bl-038/src-inst-014). The director remediated it
same-day via ev-director-0005; the re-run duplicate sweep is clean. All other
checks passed unchanged. Original findings preserved below; remediation addendum
at the end.

## 1. Deterministic checks — PASS

- `python tools/lifecycle_selftest.py` — **PASSED, 41 checks**, exit 0.
- `python tools/merge_validate.py coverage` — **"validation clean"**, exit 0
  (includes full validation plus the coverage gate: matrix + roster receipt +
  Nation-bound search evidence + baseline closure).
- Note: the wa-inland lane report mentions 26 pre-existing schema findings in the
  id-lane slices at its write time; those are resolved — current merged validation
  is clean.

## 2. Coverage-matrix audit — PASS

Programmatic whole-population checks (not sampled):

- 43 matrix rows vs 43 `NATIONS.md` rows: exact 1:1, no duplicates, no missing,
  no extras. Statuses: found 26, searched-not-found 13, excluded 4 (sums to 43).
- Every cited `search_events` id exists in the merged access log, is
  `action: "search"`, and carries the matching `nation_id`. **No search event is
  reused across Nations.**
- Every `found` row's `source_ids` exist in the register with the matching
  `nation_id` and candidate/unresolved-baseline status.
- Honesty sweeps: **no searched-not-found Nation has any register row at all**
  (unmarked or otherwise); no excluded Nation has any candidate row. The three
  register rows absent from `found` matrices (src-wai-003, src-or-005, src-id-004)
  are exactly the director-marked duplicates — correct omissions.
- All 4 `excluded` rows spot-checked (lower-elwha-klallam/ev-wa-coastal-005,
  quinault/ev-wa-coastal-014, skokomish/ev-wa-coastal-018,
  shoshone-paiute/ev-id-sear005): each search event exists, is Nation-specific
  (query names the Nation), carries the right `nation_id`, and each cited excluded
  register row (src-wac-011, src-wac-012, src-wac-007, src-id-007) exists and
  supports the status.
- Seeded 5-of-13 searched-not-found sample (sorted list, `rng.sample(snf, 5)`):
  **sauk-suiattle, hoh, makah, shoalwater-bay, stillaguamish**. Each has exactly
  one logged Nation-specific search event
  (`search://native-web?q=<Nation name> research data policy code IRB`), correct
  `nation_id`, and zero register rows — status supported.

Non-blocking observation: wa-coastal logged one generic query per Nation while
wa-inland logged three per Nation; the charter sets no minimum, but the
searched-not-found conclusions in wa-coastal rest on thinner search effort.

## 3. Publication-intent spot-check (N=10, seed 20260721, lane × layer) — PASS

Population: candidate rows in the five new lanes, minus director-marked duplicates
(48 rows, 12 non-empty lane×layer strata). Allocation: 1 per each of the 8
multi-row strata + seeded pick of 2 of the 4 singleton strata. Sample and verdicts
(URL/issuer plausibility only; nothing fetched):

| stratum | source_id | verdict |
|---|---|---|
| id/nation | src-id-005 | Plausible — sbtribes.com official domain, slug matches title. Notes honestly flag it is a website privacy policy needing scope review. |
| institutional/academic | src-inst-019 | Plausible — cih.jhu.edu (JHU Center for Indigenous Health) resource page, CMS-shaped path. |
| institutional/federal | src-inst-015 | Plausible — noaa.gov/sites/default/files Drupal path; matches the real signed May 2024 NOAA IK guidance. |
| institutional/intertribal | src-inst-008 | Plausible — NCAI resolution ABQ-19-01 on the legacy wwwe.ncai.org host; register honestly flags the legacy host for review. |
| institutional/research-governance | src-inst-010 | Plausible — usindigenousdatanetwork.org wp-content upload of the USIDSN brief. |
| or/nation | src-or-002 | Plausible — ctclusi.org issuer PDF, title matches Tribal Code Ch. 1-10 Research Regulation. |
| wa-coastal/nation | src-wac-008 | Plausible — swinomish-nsn.gov official Tribal Archive research-request page. |
| wa-inland/nation | src-wai-007 | Plausible — spokanetribe.com official Preservation Program page; intent statement matches page purpose, legal-force caveat recorded. |
| wa-coastal/academic | src-wac-010 | Plausible — nwic.edu-hosted IRB Policy 806 PDF; intent cites the snippet's Upper Skagit linkage. |
| wa-inland/state | src-wai-004 | Plausible — commerce.wa.gov press release about the executed Kalispel MOU; intent honestly claims only the release, not the MOU text, and `official_source: false` is correctly set. |

No over-claiming found; every sampled row records verification as deferred.
Improvement over baseline boilerplate: new-lane `publication_intent` is per-item
evidence, not a stock sentence.

## 4. Exclusions audit — PASS

Six excluded rows corpus-wide, all with concrete reasons:

- src-wai-001 (yakama, excluded-gated) — NARF/NILL result states the Nation has
  not given permission for full-text online publication. Correct default-refuse;
  report correctly instructs no bypass attempt.
- src-id-007 (shoshone-paiute, excluded-ambiguous) — same NARF no-permission
  pattern. Correct refusal.
- src-wac-007 (skokomish) — official index marks the records/FOIA item "reserved";
  no published operative instrument establishable. Correct.
- src-wac-009 (tulalip) — Pew fact sheet about the Tulalip–WA DOH DSA; refused
  until deliberate publication of the agreement itself is verified. Correct
  conservative call (the found-status Nation retains other candidates; matrix
  notes disclose the excluded lead).
- src-wac-011 (lower-elwha-klallam) — DSHS category page, not an instrument. Correct.
- src-wac-012 (quinault) — UW repository bitstream referencing a protocol; cannot
  establish issued-instrument or deliberate-publication status. Correct.

No over-exclusion found. Under-exclusion check: third-party code holdings kept as
candidates (src-wac-001 ecode360, src-wac-002/003 NILL, src-id-006 municode) all
carry explicit needs-review notes; distinguishable from the excluded NARF rows,
whose search results affirmatively stated lack of permission. Defensible.

Non-blocking observation: src-wai-001 (excluded-gated) and src-id-007
(excluded-ambiguous) record materially the same evidence pattern ("Tribe has not
given permission") under different sub-statuses; harmonize at DS-2.

## 5. Duplicate-resolution audit — **FAIL (blocking)**

Director-marked pairs — all four verified correct:

- src-or-005 → src-bl-002: both CTUIR office-of-legal-counsel codes pages,
  alternate URLs, same issuer. Correct; official baseline row retained.
- src-wai-003 → src-wai-002: Thorpe (OU) secondary holding of Colville Ch. 6-6 vs
  official colvilletribes.com archives-records lead. Content-level (not URL-level)
  pairing; correct under charter official-source preference.
- src-id-004 → src-bl-012: nezperce.org/resources/tribal-code/ vs
  www.nezperce.org/government/tribal-code/ — same code index, alternate URL.
  Correct; the deliberate retention of src-id-003 alongside src-bl-013 (newer
  direct document lead) is reasoned and sound.
- src-inst-020 → src-wac-010: byte-identical URL (NWIC Policy 806 PDF); wac row
  correctly retained for its upper-skagit linkage.

Unresolved-pair sweep (normalized URL, and normalized title+entity, across all
107 rows minus marked duplicates):

- **BLOCKING: src-bl-038 (baseline) and src-inst-014 (institutional) share the
  identical URL `https://www.bia.gov/service/tribal-consultations/tribal-data-priorities`,
  same entity (Bureau of Indian Affairs), same page ("BIA consultation on Tribal
  Data Priorities" / "Tribal Data Priorities"). Both are active candidates; no
  register-status event resolves the pair.** The director's dedup pass
  (ev-director-0004) reviewed src-inst-003/013/017 vs src-bl-033/034 but not
  src-inst-014 vs src-bl-038. PLAN.md line 96 makes "duplicates resolved" a DS-1
  gate criterion, so this is blocking.
  - Files: `sources/register.jsonl` (src-bl-038 at line 38; src-inst-014 at
    line 100), slices `sources/register.baseline.jsonl` line 38 and
    `sources/register.institutional.jsonl` line 14.
  - Remediation: one director `register-status` event marking one row (suggest
    src-inst-014, retaining the baseline row per precedent) `duplicate` with
    `duplicate_of=src-bl-038`, then re-run `merge_validate.py coverage`.

No same-title+entity or cross-lane same-title pairs remain otherwise.

## 6. Lane-report completeness — PASS

All six reports exist in `reviews/lane-reports/` (baseline, wa-coastal, wa-inland,
or, id, institutional, all dated 2026-07-21). Each states records produced,
searches run, exclusions, host-allowlist requests, and human-review flags, and
reports a clean validation run. Counts cross-checked against slices: baseline
53/106, wa-coastal 12 rows (8 candidate + 4 excluded-ambiguous)/37 events,
wa-inland 7 rows/19 events, or 8 rows/17 events, id 7 rows/12 events,
institutional 20 rows/36 events — all match. wa-inland correctly self-flagged
src-wai-003 for director dedup (which happened, ev-director-0002).

## Non-blocking findings (carry to DS-2)

1. Mojibake characters in several titles (src-wai-002 "Archives & Records �",
   src-wac-007/009, src-id-007) — encoding artifacts; fix at DS-2 relocation.
2. Excluded sub-status inconsistency: wai-001 gated vs id-007 ambiguous for the
   same NARF no-permission evidence.
3. wa-coastal single-query-per-Nation search depth is thin support for its 11
   searched-not-found conclusions.
4. Baseline audit carry-overs remain open (bl-020/021 entity, bl-053 truncated
   URL, baseline publication_intent boilerplate).

## Recommendation (original, superseded by addendum)

**FAIL the gate as-stands; conditional re-pass available.** Every check except
duplicate resolution passes, and the failure is a single missed pairing with a
one-event remediation. After the director marks src-inst-014 (or src-bl-038)
duplicate and `merge_validate.py coverage` re-runs clean, the gate can be
re-certified by re-running only check 5's sweep — no re-sampling required
(the sampled populations are unchanged by the fix).

## Remediation addendum — 2026-07-21 (same session)

The director remediated the check-5 blocking finding exactly as prescribed:

- **ev-director-0005** (`provenance/access-log.director.jsonl` line 5, merged into
  `provenance/access-log.jsonl`), ts 2026-07-21T17:44:54Z, actor claude/director:
  `register-status` marking **src-inst-014 → duplicate, duplicate_of=src-bl-038**,
  baseline row retained per director precedent. Notes cite this audit's check-5
  finding. Event verified present in both the director slice and the merged log.
- `python tools/merge_validate.py coverage` re-run by this auditor: **"validation
  clean"**, exit 0.

Check-5 re-sweep (this auditor, against the current merged register + log;
duplicate set now DERIVED from register-status events, not hardcoded — last state
wins per source_id):

- Duplicate-marked set from events: src-id-004, **src-inst-014**, src-inst-020,
  src-or-005, src-wai-003 — the new event resolves the flagged pair; all five
  `duplicate_of` targets exist in the register.
- Active population (candidate/unresolved-baseline minus event-marked duplicates):
  96 rows. Normalized same-URL sweep: **zero groups**. Normalized
  title+entity sweep: **zero groups**. No unresolved duplicate pair remains.

Checks 1–4 and 6 are unaffected by the fix (sampled populations unchanged;
src-inst-014 was not in any sample). **Check 5: PASS after remediation.**

### Revised overall gate recommendation

**PASS — DS-1 gate certified.** All six checks pass; zero blocking findings
outstanding; zero charter violations. Non-blocking findings above carry to DS-2.
