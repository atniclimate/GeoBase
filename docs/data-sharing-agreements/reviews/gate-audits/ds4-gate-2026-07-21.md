# DS-4 gate audit — 2026-07-21

**Verdict: FAIL (2 blocking findings).**
Auditor: Claude verification lane (DS-4 gate, per `prompts/claude-audit.md`).
Seed: **20260721** (stratified, recorded below). Population: 64 summaries
(`summaries/*.md`), 8 theme pages (`wiki/*.md`), catalog 64 records / 359
claims, corpus bytes per `corpus/MANIFEST.jsonl`.

## Deterministic whole-population checks

- `python tools/lifecycle_selftest.py` — **PASSED** (41 checks).
- `python tools/merge_validate.py validate` — **FAILED, exit 1, 48 findings**
  (see Blocking finding 1).

## Blocking findings

### 1. BLOCKING — broken provenance chain for the four ds4-b theme pages (Charter §5)

`merge_validate.py validate` reports 48 findings, all of the form
`artifact_path missing on disk`. Every `summarize` event the ds4-b lane
logged for its four theme pages points at a lane-suffixed artifact that no
longer exists; the final `wiki/*.md` pages on disk have **no** summarize
events citing them:

| Missing artifact_path | Events | Range |
|---|---|---|
| `wiki/publication-approval.ds4-b.md` | 9 | ev-ds4-b-0069 .. ev-ds4-b-0077 |
| `wiki/benefit-sharing-data-return.ds4-b.md` | 14 | ev-ds4-b-0078 .. ev-ds4-b-0091 |
| `wiki/enforcement-jurisdiction.ds4-b.md` | 19 | ev-ds4-b-0092 .. ev-ds4-b-0110 |
| `wiki/ip-tk-protection.ds4-b.md` | 6 | ev-ds4-b-0111 .. ev-ds4-b-0116 |

The bytes-to-statement chain required by Charter §5 does not close for
these four pages: the events exist but their artifact is gone (renamed to
the unsuffixed final path without a correction/supersede record). Findings
from the validator are blocking by contract. Remedy is a director act
(correction events re-pointing artifact_path, or equivalent), not an edit
by this lane.

### 2. BLOCKING — uncorrected Title 6 currentness on the enforcement theme page (Charter §8; correction ev-director-0083)

`wiki/enforcement-jurisdiction.md:37` presents
`nez-perce-revised-code-title-6:c027` with legal status **`enacted-current`**.
The authoritative correction `ev-director-0083` (with review downgrade
`ev-director-0084`) rules that the merged catalog's `enacted-current` /
`date_amended_through 2023-09-26` overstate component-level evidence
(Gaming Ordinance only) and that the corrected reading is
**`enacted-amended-check-version`** with title-wide currentness left to
human review. Every other DS-4 use complies:

- `summaries/nez-perce-revised-code-title-6.md` — corrected status plus an
  explicit "Currentness correction" callout citing ev-director-0083/0084. OK.
- `wiki/consent-fpic.md:62`, `wiki/ownership-control.md:60`,
  `wiki/redisclosure-confidentiality.md:70` — `enacted-amended-check-version`. OK.
- `wiki/enforcement-jurisdiction.md:37` — **uncorrected**, and the page
  carries no currentness caveat for this record. This presents material of
  unverified currentness as a Nation's current enacted requirement —
  a Charter §8 violation, and the same class of correction non-compliance
  the DS-3 gate treated as blocking.

## Out-of-scope observations (not DS-4 population; flagged for director)

- `wiki/nations/yakama.md:28` presents `yakama-water-code:c002` with the
  uncorrected `must-shall` modal and the overstated conditions clause,
  contrary to ev-director-0140 ("Downstream DS-4+ uses of c002 must present
  the discretionary modal"). `wiki/nations/nez-perce.md:80` likewise shows
  Title 6 as `enacted-current`. The Nation display pages appear to be
  generated from merged state without applying correction events
  (`nation_pages.py`). Not DS-4 outputs, but the same corrections regime
  should govern them before any outreach use.

## Sample design and sizes (seed 20260721)

Strata: theme (8) × issuer type (Nation-linked vs non-Nation), 2 rows per
stratum where the stratum exists, plus forced includes. RNG:
`random.Random(20260721)`, selection order deterministic (themes in fixed
order, `random.sample`).

- **Theme-page statements sampled: 34** (target ≥25), covering all 8 theme
  pages, both strata on every page, including forced
  `yakama-water-code:c002` (wiki/ownership-control.md:21) and
  `yakama-water-code:c010` (wiki/redisclosure-confidentiality.md:21) — 2
  yakama-water-code statements as required — and
  `nez-perce-revised-code-title-6:c007` (wiki/consent-fpic.md:66), whose
  page section carries the corrected `enacted-amended-check-version`
  status.
- **Summaries sampled: 14** (target ≥12): 7 Nation-linked per catalog
  `nation_id` (preservation-program,
  quileute-law-and-order-code-articles-i-xii, archives-and-collections,
  klamath-special-use-permit, coeur-dalene-research-permit-application,
  wac-006, wai-005), 5 non-Nation (inst-003, inst-016, bl-022, inst-002,
  bl-023), plus forced yakama-water-code and
  nez-perce-revised-code-title-6. All **92** claim-table rows in these
  14 summaries were checked.
- **Byte-verified claims: 12** (statement → claim_id → catalog →
  content_version bytes opened and read): yakama-water-code:c002 and c010;
  siletz-research-ordinance:c004; nwic-irb-policy-806:c008;
  colville-chapter-6-6-research-regulation:c002;
  klamath-special-use-permit:c001; r3-005:c007;
  nez-perce-revised-code-title-6:c007; d2bl-001:c021; inst-003:c008;
  coeur-dalene-research-permit-application:c001; wac-182-125-0100:c009.
  All faithful to the retained bytes; no omitted qualifier, no broadening,
  no modal drift found in the sample.

### Corrected-reading spot checks (required by gate charge)

- **yakama-water-code:c002** — corpus bytes
  (`corpus/.../yakama-water-code/v1-d1634cc3.pdf`, 60.05.05 preamble PDF
  p.10, item (h) p.11) read "The Director and, under his or her direction,
  the Water Code staff **may**: … (h) Collect and maintain statistical
  records…". Both DS-4 uses (`summaries/yakama-water-code.md:20` and
  `wiki/ownership-control.md:21`) present `may-permissive` with the
  ev-director-0140 correction noted. **Compliant.**
- **nez-perce-revised-code-title-6** — summary and three of four theme-page
  uses carry the corrected currentness reading; the fourth is Blocking
  finding 2.

## Whole-population checks (all clean)

- **Traceability / fidelity, mechanical, 100% of statement rows** (all 8
  theme pages and all 64 summaries): every claim_id resolves to the catalog;
  modal, category, and cite appear verbatim in every row (yakama c002
  measured against the corrected modal). Sampled rows additionally verified
  to restate the catalog `claim` and `conditions` text verbatim (34 wiki
  rows + 92 summary rows; zero rewrites other than the required c002
  correction rewrite). No statement without a claim_id states a requirement;
  no over-quotation observed (restatements are catalog paraphrases, quotes
  are sparing and attributed).
- **Theme completeness**: category-to-page reconciliation — consent-fpic
  39/39, ownership-control 36/36, review-board 32/32,
  redisclosure-confidentiality 54/54 (+7 cross-category rows admitted by the
  page's stated explicit-mention rule, each showing its true category),
  publication-approval 14/14, benefit-sharing-data-return 25/25,
  enforcement-jurisdiction 36/36 (+8 r3-002 claims in a clearly framed
  "separate government-to-government control plane" prose section),
  ip-tk-protection 5/5 (+5 cross-category claims with true categories
  visible in the matrix). No cataloged Nation with claims in a theme's
  categories is missing from that theme page.
- **Searched-not-found**: all 13 `searched-not-found` Nations in
  `sources/coverage-matrix.jsonl` (cow-creek, cowlitz, hoh,
  jamestown-sklallam, kootenai-idaho, lummi, makah, nooksack, samish,
  sauk-suiattle, shoalwater-bay, squaxin-island, stillaguamish) are listed
  as such on every one of the 8 theme pages.
- **Banners**: DRAFT banner at top and non-discovery-≠-absence footer
  present on all 64 summaries and all 8 theme pages (0 failures).
- **Handling rules (flagged docs bl-038, d2bl-001, inst-003, inst-004,
  inst-011, inst-013, wac-008, wai-005)**: full-corpus sweep of
  summaries/ + wiki/ found zero personal email addresses, zero phone
  numbers, zero individual names, zero signature descriptions from flagged
  documents; all 8 flagged summaries carry the "Handling rule applied:
  institution-not-person" statement. The d2bl-001 source bytes contain a
  personal email in the provider table (PDF p.30); the d2bl-001:c021
  restatements correctly omit it. All "signature" mentions in outputs are
  policy content about signature *requirements* (Colville/Nez Perce permit
  rules), not descriptions of signatures.
- **Klamath Exhibit A**: zero site-location content anywhere in
  summaries/ or wiki/; `summaries/klamath-special-use-permit.md` contains
  no Exhibit A, parcel, coordinate, or site-description content.

## Disposition

FAIL. Two blocking findings, both remediable by director action without
touching summary/theme content fidelity, which the sample found sound:

1. Close the provenance chain for the four ds4-b theme pages (correction
   events re-pointing ev-ds4-b-0069..0116 artifact_paths to the final
   `wiki/*.md` paths, or restore/supersede properly), then re-run
   `merge_validate.py validate` to zero findings.
2. Fix `wiki/enforcement-jurisdiction.md:37` to the ev-director-0083
   corrected status `enacted-amended-check-version` (with the currentness
   caveat), consistent with the other three theme pages and the summary.

Re-audit after remediation may be limited to: validate re-run, the
corrected enforcement-jurisdiction row, and a hash check that no other
output changed.

---

## Director remediation addendum — 2026-07-21 (claude/director)

- **Blocking 1 (orphaned artifact_paths):** the four ds4-b lane drafts were
  restored verbatim at their `wiki/<theme>.ds4-b.md` paths (immutable lane
  artifacts; all 48 summarize events resolve again), and the promotion of
  each draft to its published `wiki/<theme>.md` page is now recorded as
  director events `ev-director-0189..0192`. `merge_validate.py validate`:
  zero findings.
- **Blocking 2 (Title 6 correction non-compliance):** the
  `nez-perce-revised-code-title-6:c027` row in
  `wiki/enforcement-jurisdiction.md` now reads
  `enacted-amended-check-version` with the ev-director-0083 currentness
  caveat; the fix is documented inside promotion event `ev-director-0191`.
  The lane draft retains the original restatement as the historical
  artifact.
- **Out-of-scope observation (Nation pages ignore corrections):** fixed at
  the generator — `tools/nation_pages.py` now renders every director
  correction event for a cataloged (doc_id, content_version) as an
  authoritative-reading blockquote on the document's section and flags any
  claim_id named in a correction as "⚠ superseded — see correction above".
  Regenerated: yakama.md now presents c002's corrected discretionary
  reading; nez-perce.md carries the Title 6 currentness correction.

**Gate disposition after remediation: PASS** (both blocking findings
remediated and re-verified: validate zero findings; corrected Title 6 row
confirmed on the published page; correction surfacing confirmed on the
regenerated Nation pages). The audit's whole-population fidelity results
(claim-verbatim rows, zero handling-rule leaks, complete banners,
completeness reconciliation 359/359) stand unchanged.
