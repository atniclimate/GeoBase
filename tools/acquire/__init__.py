"""tools/acquire — public-data acquisition for GeoBase nodes (F4).

Out-of-product tooling: turns an AOI polygon into a staging directory of
public federal data that `geopack package` ingests through the existing
pipeline. Never a product dependency, never a required CI check. See
`tools/acquire/README.md` for the operator walkthrough and the full Tier-0
attribution + provenance record.
"""
