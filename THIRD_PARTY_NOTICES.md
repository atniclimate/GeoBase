# Third-Party Notices

GeoBase incorporates or adapts third-party material as recorded below. Code is
Apache-2.0 (`LICENSE`); TSDF framework content under `spec/tsdf/` remains
CC-BY-NC-SA 4.0 (`spec/tsdf/ATTRIBUTION.md`) and is **not** relicensed by
inclusion. Geospatial **data** never enters this repository.

This file has two parts: **adapted code/patterns** (things vendored or
adapted from another project) and **public-data source attributions** (the
Tier-0 sources `tools/acquire` fetches, whose attribution + provenance the
project commits to recording). The dependency-license audit (cargo + pnpm
graphs) is generated separately — see "Dependency license audit" at the end.

Created 2026-07-16 (Phase A / early Backlog B-1). This is a living file; each
vendoring or adaptation adds an entry with its pinned source.

---

## Adapted code & patterns

### mazzap_veil — fetch safety discipline (adapted, not vendored)
- **Source:** `github.com/zymazza/mazzap_veil`
- **License:** MIT
- **What we adapted:** the fetch safety discipline (advertised-size check,
  free-disk headroom, refuse-oversized, clip-to-AOI, discard raw archives) and
  the LANDFIRE LFPS async submit→poll→download pattern, reimplemented in
  `tools/acquire/safety.py` and `tools/acquire/fetchers.py`. No mazzap_veil
  code is copied verbatim; the *approach* is adapted and credited here.
- **Pin:** record the exact commit adapted from when the LFPS fetcher is wired
  (the safety module is a clean-room reimplementation of the documented rules).

---

## Public-data source attributions (Tier-0, via `tools/acquire`)

All sources below are **Tier 0** under the TSDF (public federal data) and may
be used freely **with attribution and provenance recorded**. The authoritative,
per-source record is `tools/acquire/README.md`; `tools/acquire/sources.py` is
its machine-readable twin. Summarized here:

- **USGS 3DEP Digital Elevation Model** — "Elevation data courtesy of the U.S.
  Geological Survey 3D Elevation Program (3DEP), The National Map." Public
  domain (17 U.S.C. § 105). Via TNMAccess.
- **USGS 3DEP LiDAR point clouds** — "LiDAR point clouds courtesy of the USGS
  3D Elevation Program (3DEP)." Public domain; AWS Open Data
  `usgs-lidar-public` mirror.
- **LANDFIRE fuels & vegetation** — "Fuels and vegetation data from LANDFIRE, a
  joint program of the USDA Forest Service and U.S. Department of the
  Interior." Public domain; cite program + product version.
- **USGS NHDPlus HR / Watershed Boundary Dataset** — "Hydrography from the USGS
  National Hydrography Dataset Plus High Resolution (NHDPlus HR) and the
  Watershed Boundary Dataset (WBD)." Public domain (17 U.S.C. § 105).

---

## Candidate future vendoring (recorded from the DG-3 spike, not yet adopted)

### Whitebox Next Gen `wbtools_oss` — geomorphometry tools (evaluated 2026-07-16)
- **Source:** `github.com/jblindsay/whitebox_next_gen`, crate `wbtools_oss`
- **Rev evaluated:** `a377e25ac6fbe3ef43f598048fa5a32119fb11b5`
- **License:** `MIT OR Apache-2.0` at the crate level (each `Cargo.toml`'s
  `license` field). Caveat: no top-level LICENSE file and no per-file SPDX
  headers (0/79 `.rs` files) — the grant is crate-level only. See
  `docs/DECISIONS.md` 2026-07-16 DG-3 spike.
- **Status:** NOT vendored. DG-3 (adopt Next Gen vs the pre-approved
  legacy-MIT WhiteboxTools fallback) is owner-open. If adopted, vendor
  `wbtools_oss` + its open `wb*` deps at the pinned rev, **excluding**
  `wbtools_pro_shim` and `wblicense_core`, and expand this entry with the
  per-crate grants.

---

## Dependency license audit

The full transitive license inventory of the cargo and pnpm dependency graphs
is generated by the license-audit CI job (see `.github/workflows/`). It is not
hand-maintained here; this section will link to the generated artifact once the
audit job records its first run.
